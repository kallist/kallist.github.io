# V2 review notes

## Scope

Homepage visual and navigation system only. Project facts, case routes, résumé identity, static export and hosting workflow remain intact. No phone number, fabricated degree or clinical claim was added.

## Decisions

- The photo discussed during implementation is **not used**. After the user corrected the direction, the local download was removed and a numeric SVG illustration was authored instead. Its visual reference and rights are listed in `ASSET_INVENTORY.md`.
- The education section displays school, major, status and dates in Chinese only. The résumé continues to contain the owner's real name.
- V1.1's two mini artwork crops are removed. Visual Practice now has one dominant work and a restrained caption.

## Validation boundary

Record exact local and Hosted CI results in the PR body after the latest commit. Local viewport screenshots do not establish physical phone or cross-browser behavior. This Draft PR must remain unmerged and undeployed.

## Visual depth pass review

- Selected Work no longer repeats one alternating text/image template. RepoBound presents a registered source block; CueParcel puts the five-step route above a broad screenshot; Agent Studio uses six coordinates and a wide runtime field; Skin Lesion leads with three large evaluation values and keeps the clinical caveat visible.
- The screenshots occupy approximately 50–90% of the usable desktop width depending on scene. Internal guides and registration crosses connect the fixed rails to content. A numeric reading marker travels in the right rail while native scrolling remains intact.
- Character fields now encode four distinct densities and paths. They stay behind content; the CueParcel contrast and Skin Lesion metric scale were corrected after visual capture exposed CSS cascade conflicts.
- INDEX fills the site width inside continued rails, with a visible grid and active chapter. Navigation remains a native dialog. Word feedback uses reusable semantic spans and CSS; no per-word JavaScript listener was added.
- The 390px layout uses 30px rails and retains a 44px INDEX target. Education remains a character-drawn gate with Chinese identity. One artwork remains in Visual Practice and the graphite ending is intact.
- Remaining limits: Chromium viewport images are not physical device or cross-browser evidence. The restrained fields are intentionally secondary to readable project content. See `visual-qa/v2/README.md` for capture and interaction checks.
